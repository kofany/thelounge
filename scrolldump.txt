html, body {
    overscroll-behavior-y: none;
    position: fixed;
    overflow: hidden;
}

::-webkit-scrollbar {
    height: 5px;
    width: 5px;
}

::-webkit-scrollbar-track {
    background-color: var(--scrollbar-track);
    margin-bottom: 8px;
}

::-webkit-scrollbar-thumb {
    background-color: var(--scrollbar-thumb);
}

::-webkit-scrollbar-thumb:hover {
    background-color: var(--scrollbar-thumb-hover);
}

.scroller {
    scrollbar-color: var(--scrollbar-thumb) var(--scrollbar-track);
    scrollbar-width: auto;
}

.scroller.f {
    scrollbar-width: thin;
}

.scroller::-webkit-scrollbar-track {
    margin-top: 0px;
    margin-bottom: 0px;
}

::placeholder {
    color: var(--placeholder-font);
    opacity: 1;
}

:-ms-input-placeholder {
    color: var(--placeholder-font);
}

::-ms-input-placeholder {
    color: var(--placeholder-font);
}

.spinnerHolder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    position: absolute;
}
.spinner {
    display: inline-block;
    width: 80px;
    height: 80px;
    position: absolute;
}
.spinner div {
    display: inline-block;
    position: absolute;
    left: 8px;
    width: 16px;
    background: #fff;
    animation: spinner 1.2s cubic-bezier(0, 0.5, 0.5, 1) infinite;
}
.spinner div:nth-child(1) {
    left: 8px;
    animation-delay: -0.24s;
}
.spinner div:nth-child(2) {
    left: 32px;
    animation-delay: -0.12s;
}
.spinner div:nth-child(3) {
    left: 56px;
    animation-delay: 0;
}
@keyframes spinner {
    0% {
        top: 8px;
        height: 64px;
    }
    50%, 100% {
        top: 24px;
        height: 32px;
    }
}

.typing {
    display: inline-block;
    position: relative;
    width: 22px;
    height: 20px;
}
.typing div {
    position: absolute;
    top: 7px;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #fff;
    animation-timing-function: cubic-bezier(0, 1, 1, 0);
}
.typing div:nth-child(1) {
    left: 2px;
    animation: typing1 0.6s infinite;
}
.typing div:nth-child(2) {
    left: 2px;
    animation: typing2 0.6s infinite;
}
.typing div:nth-child(3) {
    left: 8px;
    animation: typing2 0.6s infinite;
    }
.typing div:nth-child(4) {
    left: 14px;
    animation: typing3 0.6s infinite;
}
@keyframes typing1 {
    0% {
        transform: scale(0);
    }
    100% {
        transform: scale(1);
    }
}
@keyframes typing3 {
    0% {
        transform: scale(1);
    }
    100% {
        transform: scale(0);
    }
}
@keyframes typing2 {
    0% {
        transform: translate(0, 0);
    }
    100% {
        transform: translate(6px, 0);
    }
}

.copy {
    font-size: 0;
    opacity: 0;
    width: 0;
}

div.logo {
    width: 50px;
    height: 40px;
    background-image: url(/assets/main/images/logo);
    background-size: 20px;
}

.inline.link {
    display: inline;
    word-break: break-all;
}

.inline.link, .inline.channel {
    text-decoration: none;
    color: var(--accent);
}

.inline.link:hover, .inline.channel:hover {
    opacity: 0.7;
}

span.accent {
    color: var(--accent);
}

div.accent {
    background-color: var(--accent);
}

.fade {
    height: 8px;
    width: 20px;
    background: red;
    position: absolute;
    z-index: 1;
    background: -webkit-gradient(linear,left top,left bottom,from(rgba(45,45,45,0)),to(var(--chat-bg)));
    background: linear-gradient(180deg,rgba(45,45,45,0) 0,var(--chat-bg));
}

.contextMenu, .popover {
    border-radius: 10px;
    -webkit-user-select: none;
    user-select: none;
}

.networkHeader .label, .channels .channel .label, .channels .channel .count, .userHeader, .users .user {
    text-overflow: ellipsis;
    white-space: nowrap;
    overflow: hidden;
    padding-right: 5px;
}

.networkHeader, .userHeader {
    background-color: var(--list-header-bg);
}

.networkHeader.selected, .channels .channel.selected {
    background-color: var(--list-item-selected-bg);
}

.networkHeader.selected:before, .channels .channel.selected:before {
    width: 5px;
    background-color: var(--accent);
    content: "";
    display: block;
    position: absolute;
    left: 0px;
}
.channels .channel.selected:before {
    left: -5px;
}

.networkHeader.selected:before {
    height: 40px;
}

.networkHeader * {
    pointer-events: none;
}

.channels .channel.selected:before {
    height: 26px;
}

#channelList .menuHolder {
    display: flex;
    justify-content: space-between;
    background-color: var(--topic-hover-bg);
    box-shadow: 0 -1px 8px rgb(0 0 0 / 30%);
    position: relative;
}

#channelList .menu {
    height: 40px;
    width: 100%;
    display: flex;
    justify-content: center;
    background-color: var(--input-bg);
}

#channelList .menu .item.add {
    height: 40px;
    width: 40px;
    background-image: url(/assets/client/images/add);
    background-size: 20px;
}

#channelList .menu .item.settings {
    height: 40px;
    width: 40px;
    background-image: url(/assets/client/images/settings);
    background-size: 20px;
}

#channelList .menu .item.logout {
    height: 40px;
    width: 40px;
    background-image: url(/assets/client/images/logout);
    background-size: 20px;
}

#userList {
    border-left: 1px solid var(--border);
}

#channelList, .networks .network, #userList, .userSection {
    background-color: var(--list-bg);
    color: var(--primary-font);
}

.channels .channel .label {
    flex-grow: 1;
}

.invisible {
    opacity: 0 !important;
    visibility: hidden !important;
}

.channels .channel .count {
    flex-shrink: 0;
    opacity: 1;
    transition: all 0.2s ease;
}

.channels .channel .count.highlights {
    color: var(--accent);
}

.networkHeader .label, .channels .channel .label, .users .user.away .label {
    opacity: 0.5;
}

.network.connected .networkHeader .label, .network.connected .channels .channel.joined .label {
    opacity: 1.0;
}

.resizeBorder {
    width: 5px;
    height: 100%;
    z-index: 1;
}

.resizeBorder.r {
    right: -5px;
    float: right;
}
.resizeBorder.l {
    left: -5px;
    float: left;
}

.resizeBorder:hover {
    cursor: ew-resize;
}

.absolute {
    position: absolute;
}

.left {
    left: 0;
    float: left;
}

.right {
    right: 0;
    float: right;
}

#content {
    height: 100%;
    width: 100%;
    min-height: 160px;
    min-width: 1000px;
    display: flex;
}

#channelList {
    height: 100%;
    width: 200px;
    flex-grow: 0;
    box-shadow: 0 1px 8px rgba(0,0,0,0.3);
    position: relative;
    z-index: 3;
    -webkit-user-select: none;
    user-select: none;
    border-right: 1px solid var(--border);
    flex-grow: 0;
    flex-shrink: 0;
}

.networks.sorting {
    display: flex;
    align-content: center;
    flex-wrap: wrap;
}

.networks {
    height: calc(100% - 40px);
}

.users {
    height: calc(100% - 20px);
    padding-bottom: 10px;
}

.networks, .users {
    padding-top: 10px;
}

.networks, .users {
    overflow-y: auto !important;
    width: 100%;
    flex-grow: 1;
}

.networks .network {
    width: calc(100% - 10px);
    overflow: hidden;
    margin-bottom: 10px;
    position: relative;
}

.userSection {
    overflow: hidden;
}

.networkHeader {
    height: 40px;
    padding-left: 10px;
    display: flex;
    align-items: center;
}

.networkHeader .collapse {
    background-image: url(/assets/client/images/triangle);
    background-size: 10px;
    height: 100%;
    width: 20px;
    margin-right: 5px;
    flex-shrink: 0;
    pointer-events: all;
    transition: transform 0.2s ease;
}

.networkHeader .collapse.rotate {
    transform: rotate(-90deg);
}

.networkHeader .info {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: calc(100% - 25px);
}

.networkHeader:not(:first-child) {
    margin-top: 10px;
}

.networkHeader .labels {
    display: flex;
    flex-direction: column;
    line-height: normal;
    overflow: hidden;
    text-overflow: ellipsis;
}

.networkHeader .labels .label[data-label="nick"] {
    color: var(--secondary-font);
}

.userHeader {
    height: 30px;
    margin-top: 10px;
    line-height: 30px;
    padding-left: 10px;
    margin-left: 10px;
}

.userHeaderContents {
    width: calc(100% - 10px);
    display: flex;
    justify-content: space-between;
    overflow: hidden;
}

.network.collapsed .channels {
    height: 0px;
}

.channels .channel, .users .user {
    height: 26px;
    line-height: 26px;
}

.channels .channel *, .users .user * {
    pointer-events: none;
}

.channels .channel {
    border-left: 5px solid transparent;
    padding-left: 5px;
    position: relative;
    display: flex;
    width: 100%;
}

.channels .channel .close {
    height: 26px;
    width: 26px;
    opacity: 0;
    display: none;
}

.button.close {
    background-image: url(/assets/client/images/close);
    background-size: 20px;
    pointer-events: all;
}

.channels .channel:hover > .close {
    opacity: 1;
    display: block;
}

.channels .channel:hover > .count {
    display: none;
}

.users .user {
    padding-left: 5px;
    margin-left: 10px;
}

.channels .channel.ignored, .users .user.ignored {
    color: var(--warning);
}

.networkHeader, .users .user:hover, .channels .channel:hover, .contextMenu .action:hover {
    cursor: pointer;
}

.message[data-before]:before {
    content: attr(data-before);
    color: var(--server-font);
    background-color: var(--server-bg);
    box-shadow: inset 0 1px 0 0 var(--server-border), inset 0 -1px 0 0 var(--server-border);
    width: 100%;
    display: block;
    text-align: center;
    line-height: 20px;
}

.message[data-after]:after {
    content: attr(data-after);
    color: var(--server-font);
    background-color: var(--server-bg);
    box-shadow: inset 0 1px 0 0 var(--server-border), inset 0 -1px 0 0 var(--server-border);
    width: 100%;
    display: block;
    text-align: center;
    line-height: 20px;
}

.message[data-command=tagmsg] {
    height: 0px;
}

.msg[data-command=action] .nick:not(.inline):before {
    content: var(--action-prefix);
}

.msg[data-command=join]:before {
    content: var(--join-prefix);
    color: var(--join-prefix-color);
}

.msg[data-command=part]:before, .msg[data-command=quit]:before, .msg[data-command=kick]:before {
    content: var(--quit-prefix);
    color: var(--quit-prefix-color);
}

.msg[data-command=account]:before, .msg[data-command=chghost]:before {
    content: var(--info-prefix);
    color: var(--info-prefix-color);
}

.message[data-command=privmsg] {
    color: var(--primary-font);
}

.message[data-command=privmsg] .from.nick, .message[data-command=notice] .from.nick {
    margin-right: 8px;
}

.message[data-command=mode], .message[data-command=join], .message[data-command=part], .message[data-command=quit], .message[data-command=kick] {
    color: var(--mode-font);
}

.message[data-command=privmsg] .msg:not([data-command=action]) .nick:not(.inline):before {
    content: var(--nick-prefix);
}

.message[data-command=privmsg] .msg:not([data-command=action]) .nick:not(.inline):after {
    content: var(--nick-suffix);
}

.message[data-command=notice] .msg:not([data-command=action]) .nick:not(.inline):before {
    content: var(--notice-nick-prefix);
}

.message[data-command=notice] .msg:not([data-command=action]) .nick:not(.inline):after {
    content: var(--notice-nick-suffix);
}

.message[data-command=privmsg] .msg[data-command=action] .nick:not(.inline):after {
    content: var(--action-nick-suffix);
}

.message.selectedUser {
    color: var(--selected-font);
    background-color: var(--selected-bg);
    box-shadow: inset 0 1px 0 0 var(--selected-border), inset 0 -1px 0 0 var(--selected-border);
}

.message.highlight {
    color: var(--highlight-font);
    background-color: var(--highlight-bg);
    box-shadow: inset 0 1px 0 0 var(--highlight-border), inset 0 -1px 0 0 var(--highlight-border);
}

.message.server {
    color: var(--server-font);
    background-color: var(--server-bg);
    box-shadow: inset 0 1px 0 0 var(--server-border), inset 0 -1px 0 0 var(--server-border);
}

.message[data-command=notice] {
    color: var(--notice-font);
    background-color: var(--notice-bg);
    box-shadow: inset 0 1px 0 0 var(--notice-border), inset 0 -1px 0 0 var(--notice-border);
}

.message .day {
    display: none;
    color: var(--server-font);
    background-color: var(--server-bg);
    box-shadow: inset 0 1px 0 0 var(--server-border), inset 0 -1px 0 0 var(--server-border);
}

.message .react {
    display: flex;
}

.message .reaction {
    font-size: 18px;
    line-height: 30px;
    text-align: center;
    background-color: var(--reaction-bg);
    border: 2px solid var(--border);
    box-sizing: content-box;
    user-select: none;
    margin-right: 3px;
    margin-top: 3px;
    padding: 0px 5px 0px 5px;
}

.message .reaction:hover {
    cursor: pointer;
    background-color: var(--reaction-hover-bg);
}

.message .media {
    display: flex;
}

.message .media .inline {
    max-width: 400px;
    max-height: 250px;
    margin-right: 5px;
    margin-top: 5px;
}

#middleView {
    height: 100%;
    flex-grow: 1;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    flex-direction: column;
    position: relative;
}

#chatView {
    display: flex;
    height: 100%;
    width: 100%;
    flex: 1 1 auto;
    flex-direction: column;
}

#messageHolder {
    width: 100%;
    flex-grow: 1;
    display: flex;
    justify-content: space-between;
    position: relative;
}

#topView {
    width: 100%;
    box-shadow: 0 1px 8px rgba(0,0,0,0.3);
    border-bottom: 1px solid var(--border);
    z-index: 2;
}

#topicView {
    width: 100%;
    height: auto;
    line-height: 25px;
    justify-content: center;
    padding: 0px 8px 0px 8px;
    overflow: hidden;
    background-color: var(--topic-hover-bg);
    display: flex;
    align-items: baseline;
}

#userListContents {
    height: 100%;
    width: 100%;
}

#userFooterView {
    background-color: var(--topic-hover-bg);
    height: 20px;
    width: 100%;
    line-height: 20px;
    text-align: center;
}

#theTopic {
    width: 100%;
    font-size: 13px;
    color: var(--primary-font);
    white-space: pre-wrap;
}

#messageView {
    height: 100%;
    width: 100%;
    display: flex;
    align-items: flex-end;
    align-content: center;
    flex-direction: column-reverse;
    flex-wrap: wrap;
    position: absolute;
    z-index: 1;
    word-break: break-word;
    background-color: var(--chat-bg);
    color: var(--mode-font);
    overflow-x: hidden;
    overflow-y: scroll;
}
#messageView.f, #messageView.s {
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    overflow: hidden;
    height: 100%;
}
#messageView.f {
    padding-bottom: 8px;
}

#messages {
    margin-top: auto;
    width: 100%;
}
#messages.f {
    max-height: 100%;
    display: flex;
    flex-direction: column;
    overflow: auto;
    overscroll-behavior-y: none;
    padding-bottom: 0px;
}
#messages.s {
    max-height: 100%;
    display: flex;
    flex-direction: column;
    overflow: auto;
    overscroll-behavior-y: none;
}

#topicView.hidden + #messageHolder #messages {
    padding-top: 0px;
}

#js-copy-hack {
    position: absolute;
    z-index: 0;
}

.message {
    width: 100%;
    white-space: pre-wrap;
    position: relative;
}

.message .msg {
    overflow: hidden;
}

.message:not([data-command=notice]) .time {
    color: var(--timestamp-font);
}

.spoiler:not(:hover) .colored {
    color: inherit !important;
}

.spoiler:hover {
    color: inherit !important;
    background-color: inherit !important;
}

.message.blocked {
    color: var(--server-font) !important;
    background-color: var(--server-bg) !important;
    box-shadow: inset 0 1px 0 0 var(--server-border), inset 0 -1px 0 0 var(--server-border) !important;
}

.message.blocked .msg:after {
    content: "Message from an ignored user.";
    width: 100%;
    height: 100%;
    position: absolute;
    left: 0px;
    top: 0;
    padding-left: 103px;
    display: contents;
}
.message.blocked .msg * {
    display: none;
}

.day {
    text-align: center;
    padding: 5px 10px 5px 10px;
}

.time {
    white-space: pre;
    word-break: keep-all;
    padding: 0px 8px 0px 10px;
    display: table-cell;
}

.msg {
    padding-right: 10px;
    display: table-cell;
}

.sender, .nick, .inline {
    font-weight: bold;
    display: inline-block;
}

.inline .user {
    pointer-events: none;
}

.sender:hover, .nick:hover, .inline:hover, .link:hover {
    opacity: 0.7;
    cursor: pointer;
}

.sender, .prefix {
    pointer-events: none;
}

.colorless {
    color: inherit !important;
}

.message:first-child {
    padding-top: 3px;
}

#messages {
    padding-bottom: 10px;
}

#bottomView {
    height: 100%;
    width: 100%;
    display: flex;
    overflow: hidden;
}

#rightView {
    width: 100%;
    height: 100%;
    display: flex;
    flex-flow: column;
}

.tooltip {
    height: 25px;
    line-height: 25px;
    font-size: 12px;
    color: var(--primary-font);
    background-color: var(--tooltip-bg);
    padding: 0px 12px 0px 12px;
}

#mentionView {
    position: absolute;
    flex-shrink: 0;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
    top: 0;
    left: 5px;
    width: auto;
    z-index: 2;
    opacity: 1;
    visibility: visible;
    -webkit-user-select: none;
    user-select: none;
}

#readView {
    position: absolute;
    flex-shrink: 0;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
    top: 0;
    right: 5px;
    width: auto;
    z-index: 2;
    opacity: 1;
    visibility: visible;
    -webkit-user-select: none;
    user-select: none;
}

#pastView {
    height: 35px;
    width: 100%;
    position: absolute;
    top: -25px;
    z-index: 1;
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
    display: flex;
    justify-content: space-between;
}
#pastView:hover {
    background-color: var(--tooltip-hover-bg);
}

#inputHolder {
    width: calc(100% - 20px);
    margin: -8px 0 0px 10px;
}

#inputBackgroundView {
    z-index: 2;
    background-color: var(--input-bg);
    width: 100%;
    height: 100%;
    padding: 8px;
    border-radius: 10px;
}

#inputView {
    min-height: 40px;
    width: 100%;
    flex-grow: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 3;
    position: relative;
}

#typingView {
    position: relative;
    bottom: 0;
    height: 8px;
    width: 100%;
    color: var(--primary-font);
    font-size: 12px;
    line-height: 20px;
    display: flex;
}

#typingView.shown {
    height: 20px;
}

#typingIcon {
    width: 24px;
    height: 100%;
}

input, textarea {
    width: 100%;
    background-color: transparent;
    border: 0;
    resize: none;
    color: var(--input-font);
}

#inputField {
    min-height: 20px;
    line-height: 20px;
    font-size: 13px;
    box-sizing: content-box;
}

.textField {
    caret-color: var(--input-caret);
}

.textField:focus {
    outline: none;
}

#userList {
    height: 100%;
    width: 160px;
    flex-grow: 0;
    box-shadow: 0 1px 8px rgba(0,0,0,0.3);
    position: relative;
    z-index: 3;
    -webkit-user-select: none;
    user-select: none;
}

.hidden {
    display: none !important;
}

.switch {
    position: relative;
    display: inline-block;
    width: 26px;
    height: 16px;
    margin-right: 6px;
    flex-shrink: 0;
}
.switch.large {
    width: 42px;
    height: 25px;
    margin-right: 0;
}

.switch input { 
    opacity: 0;
    width: 0;
    height: 0;
}

.slider {
    pointer-events: all;
    position: absolute;
    cursor: pointer;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: var(--switch-bg);
    transition: .4s;
}

.slider:before {
    position: absolute;
    content: "";
    height: 14px;
    width: 14px;
    left: 1px;
    bottom: 1px;
    background-color: var(--switch-thumb);
    transition: .4s;
}

.switch.large .slider:before {
    height: 23px;
    width: 23px;
}

textarea[name=commands] {
    height: 61px;
}

input[type=checkbox] {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
}

input:checked + .slider {
    background-color: var(--switch-fill);
}

input:focus + .slider {
    box-shadow: 0 0 1px var(--switch-fill);
}

input:checked + .slider:before {
    transform: translateX(10px);
}

.switch.large input:checked + .slider:before {
    transform: translateX(17px);
}

.slider.round {
    border-radius: 34px;
}

.slider.round:before {
    border-radius: 50%;
}

#blackout {
    width: 100%;
    height: 100%;
    background-color: rgba(0,0,0,0.3);
    z-index: 90;
    position: absolute;
    top: 0;
    left: 0;
    display: none;
}

.spacer {
    height: 20px;
}

.popover {
    width: 500px;
    position: absolute;
    transform: translate(-50%, -50%);
    top: 50%;
    left: 50%;
    z-index: 100;
    background-color: var(--primary-bg);
    box-shadow: 0 8px 10px 1px rgba(0,0,0,0.14), 0 3px 14px 2px rgba(0,0,0,0.12), 0 5px 5px -3px rgba(0,0,0,0.4);
    padding: 20px;
    opacity: 0;
    display: none;
    color: var(--primary-font);
}

.popover[data-name=channelList] {
    height: 600px;
    width: 800px;
}
.popover[data-name=channelList] .formHolder {
    overflow-y: scroll;
    display: flex;
    flex-direction: column;
    height: calc(100% - 40px);
    position: relative;
}

.contextMenu {
    display: none;
    opacity: 0;
    position: absolute;
    top: 0;
    left: 0;
    z-index: 100;
    min-width: 250px;
    background-color: var(--primary-bg);
    box-shadow: 0 8px 10px 1px rgba(0,0,0,0.14), 0 3px 14px 2px rgba(0,0,0,0.12), 0 5px 5px -3px rgba(0,0,0,0.4);
    padding: 8px;
    box-sizing: border-box;
}
.contextMenu ul {
    list-style: none;
    padding: 0;
    margin: 0;
    color: var(--primary-font);
}
.contextMenu ul li {
    text-align: center;
    padding: 8px;
}
.contextMenu ul li:last-child {
    border-radius: 0 0 5px 5px;
}

.wrap {
    display: flex;
    flex-wrap: wrap;
}

.wrap * {
    width: 100%;
}

.separator {
    width: 100%;
    height: 1px;
    margin: 8px 0 8px 0;
    background-color: var(--list-border);
}

.titleHolder {
    width: 100%;
    height: 40px;
    display: table;
    line-height: 40px;
}
.titleHolder .title {
    display: table-cell;
    font-size: var(--header-font-size);
}
.titleHolder .button {
    height: 40px;
    width: 40px;
    display: table-cell;
}

.section:not(:first-child) {
    margin-top: 20px;
}

.section .title {
    font-size: var(--subheader-font-size);
    color: var(--secondary-font);
    height: 25px;
}

.fieldHolder table, .formHolder tr, .actionHolder table, .formHolder tr {
    width: 100%;
}
.fieldHolder td:nth-child(2) > input {
    width: 100%;
    height: 30px;
    text-align: right;
}

.channelListRow {
    height: 50px;
    display: flex;
    justify-content: space-between;
}

.channelListRight {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    padding: 0 10px;
}

.channelListHeader {
    height: 20px;
}

.channelListTopic {
    height: 15px;
    line-height: 15px;
    overflow: hidden;
    opacity: 0.5;
    font-size: 85%;
}

.switchHolder {
    height: 42px;
}

.actionHolder, .optionHolder {
    margin-top: 10px;
    height: 40px;
}

.actionHolder td:first-child {
    width: 100%;
}

.optionHolder {
    display: flex;
    justify-content: center;
}

.optionHolder .option {
    margin: 0 10px 0 10px;
}
 
.error {
    color: var(--negative-font);
}

.submit {
    min-width: 100px;
    width: auto;
    padding: 0px 10px 0px 10px;
    height: 40px;
    background-color: var(--accent);
    color: var(--primary-font);
    line-height: 40px;
    border-radius: 5px;
    text-align: center;
    font-size: 15px;
    text-transform: uppercase;
    -webkit-user-select: none;
    user-select: none;
}
.submit:hover {
    background-color: var(--accent-hover);
    cursor: pointer;
}

input, textarea {
    border: none;
    outline: none;
}

input::-webkit-contacts-auto-fill-button, input::-webkit-credentials-auto-fill-button {
    visibility: hidden;
    display: none !important;
    pointer-events: none;
    height: 0;
    width: 0;
    margin: 0;
}

.hover:hover {
    opacity: 0.5 !important;
    cursor: pointer;
}

.faded {
    opacity: 0.5;
}

.disabled {
    filter: grayscale(100%);
}

.fieldHolder .label {
    float: left;
}

.peek {
    display: none;
    background-image: url(https://static.thenounproject.com/png/20035-200.png);
    width: 20px;
    height: 16px;
    float: left;
    position: relative;
    left: 5px;
}

.host {
    font-size: 12px;
    opacity: 0.5;
}

.networkHeader:hover, .channels .channel:hover, .users .user:hover, .contextMenu .action:not(.warning):hover {
    background-color: var(--list-item-hover-bg);
}

.warning {
    background-color: var(--warning);
}

.warning:hover {
    background-color: var(--warning-hover); 
}

#isMobile {
    opacity: 0;
}

@media only screen and (max-width: 800px) {
    #isMobile {
        opacity: 1;
    }

    .preventTouch {
        width: 100%;
        height: 100%;
        position: fixed;
        display: none;
        z-index: 999;
    }

    #content {
        display: block;
    }

    #rightView {
        position: absolute;
        width: 100%;
        height: 100%;
        z-index: 99999;
        top: 0;
        left: 0;
    }

    #chatView {
        background-color: var(--chat-bg);
    }

    #bottomView {
        display: block;
    }

    #userList {
        position: fixed;
        top: 0;
        right: 0;
        z-index: 10;
    }

    #topView {
        z-index: 11;
    }

    #topicView {
        height: 25px;
    }
    
    #middleView {
        z-index: 11;
    }
}


nastepny plik

:root {
    --accent: #409aed;
    --accent-hover: #409aedaa;
    --warning: #db4437;
    --warning-hover: #db4437aa;

    --primary-bg: #2d2d2d;
    --chat-bg: #2d2d2d;
    --list-bg: #383838;
    --list-header-bg: #444444;
    --list-item-hover-bg: #323232;
    --list-item-selected-bg: #2b2b2b;
    --topic-bg: #282828EE;
    --topic-hover-bg: #282828FF;
    --server-bg: #282828;
    --notice-bg: #5d3737;
    --highlight-bg: #46546e;
    --selected-bg: #404040;
    --reaction-bg: #383838;
    --reaction-hover-bg: #323232;
    --monospace-bg: #202020;

    --tooltip-bg: #666666ee;
    --tooltip-hover-bg: #666666;  

    --switch-bg: rgba(255, 255, 255, 0.5);
    --switch-thumb: #fff;
    --switch-fill: var(--accent);

    --scrollbar-track: transparent;
    --scrollbar-thumb: #888888ee;
    --scrollbar-thumb-hover: #555555;

    --border: #252525;
    --form-border: #404040;
    --list-border: #404040;
    --server-border: #282828;
    --notice-border: #5d3737;
    --highlight-border: #46546e;
    --selected-border: #404040;

    --input-bg: #444444;
    --input-caret: #ffffff;

    --primary-font: #e0e0e0;
    --secondary-font: #9a9a9a;
    --timestamp-font: #6d6d6d;
    --mode-font: #666666;
    --server-font: #777777;
    --notice-font: #cc4237;
    --highlight-font: #e0e0e0;
    --selected-font: #e0e0e0;
    --positive-font: #91b14f;
    --negative-font: #db4437;

    --input-font: #cccccc;
    --placeholder-font: #777777;

    --action-prefix: "• ";
    --join-prefix: "» ";
    --join-prefix-color: #91b14f;
    --quit-prefix: "« ";
    --quit-prefix-color: #db4437;
    --info-prefix: "* ";
    --info-prefix-color: #dba937;

    --nick-prefix: "<";
    --nick-suffix: ">";
    --notice-nick-prefix: "-";
    --notice-nick-suffix: "-";
    --action-nick-suffix: "";

    --header-font-size: 20px;
    --subheader-font-size: 16px;
    --primary-font-size: 14px;
    --monospace-font-size: 13px;
}

.header {
    text-align: center;
    font-weight: bold;
    font-size: 150%;
    padding-bottom: 10px;
}

a {
    text-decoration: none;
    color: var(--accent);
}

a:hover {
    opacity: 0.7;
}

* {
    font-family: Hack, monospace;
    box-sizing: border-box;
    word-break: break-word;
    background-size: contain;
    background-position: center;
    background-repeat: no-repeat;
    border-collapse: collapse;
}

html,body {
    width: 100%;
    height: 100%;
    margin: 0;
    font-size: 14px;
}

body {
    background-color: var(--primary-bg);
    color: var(--primary-font);
}

.wrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.content {
    padding: 20px;
    text-align: center;
    max-width: 800px;
}

.left {
    text-align: left;
}

#accountHolder {
    display: flex;
    width: 100%;
    height: 100%;
    align-items: center;
    justify-content: center;
}

#accountContent {
    height: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
}

#bannerContent {
    height: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
}

#bannerButtons {
    display: flex;
}

#bannerButtons div {
    margin: 10px;
}

#accountLogoLink {
    width: 70px;
    height: 70px;
    margin-bottom: 10px;
}

#accountLogo {
    width: 100%;
    height: 100%;
    background-image: url(/assets/main/images/logo);
    background-position: center 0;
    background-size: 70px;
}

#accountMessage {
    width: 100%;
    text-align: center;
    margin-top: 10px;
}

#accountForm {
    width: 300px;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
}

#screenshotHolder {
    max-width: 800px;
    max-height: 800px;
}

#screenshot {
    width: 100%;
    height: auto;
}

#infoContent {
    bbackground-color: var(--list-bg);
    width: 100%;
}

.question .text {
    font-weight: bold;
}

.question .icon, .answer .icon {
    vertical-align: top;
    color: var(--accent);
    font-weight: bold;
    padding-right: 10px;
} 

.answer:not(:last-child) {
    margin-bottom: 20px;
}

#footerContent {
    text-align: center;
    padding: 10px;
}

.formGroup {
    width: 100%;
    padding-bottom: 10px;
}

.formGroup label {
    font-size: 12px;
    display: block;
    margin-bottom: 5px;
    color: var(--secondary-font);
}

.formGroup input {
    background-color: transparent;
    border: 1px solid var(--form-border);
    color: var(--primary-font);
}

.actionButton, .submitButton {
    width: 130px;
    height: 30px;
    line-height: 30px;
    margin-left: auto;
    margin-right: auto;
}

.actionButton[data-action=signup] {
    background-color: var(--positive-font);
}

.submitButton {
    width: 100%;
    margin-top: 0;
    text-align: center;
}

#accountForm input[type=submit], .actionButton, .submitButton {
    background-color: var(--accent);
    border: none;
    border-radius: 5px;
    color: var(--primary-font);
}

#accountForm input[type=submit]:hover, .actionButton:hover, .submitButton:hover {
    cursor: pointer;
    opacity: 0.7;
}

#accountForm input {
    width: 100%;
    height: 30px;
    outline: none;
}

code {
    background-color: var(--monospace-bg);
    padding: 2px;
}
